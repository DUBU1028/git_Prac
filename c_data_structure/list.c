#include <stdio.h>

int add_first(list, item);
int add(list, pos, item);
int delete(list, pos);
int is_empty(list);
int is_full(list);
int display(list);

int main(void)
{
	
	return 0;
}


int add_first(list, item)
{
	return 0;
}

int add(list, pos, item)
{
	return 0;
}

int delete(list, pos)
{
	return 0;
}

int is_empty(list)
{
	return 0;
}

int is_full(list)
{
	return 0;
}

int display(list)
{
	return 0;
}